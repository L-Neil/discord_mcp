apiVersion: v2
name: discord-mcp
description: Read-only Discord MCP server (Streamable HTTP) for summarizing/reporting Discord conversations from AI agents.
type: application

# Chart version. Bump on chart changes (SemVer).
version: 0.1.0
# Version of the app this chart deploys; mirrors package.json. Used as the
# default image tag when image.tag is empty.
appVersion: "1.0.0"

keywords:
  - discord
  - mcp
  - model-context-protocol
  - ai

maintainers:
  - name: ryskn
