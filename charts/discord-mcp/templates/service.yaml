apiVersion: v1
kind: Service
metadata:
  name: {{ include "discord-mcp.fullname" . }}
  labels:
    {{- include "discord-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "discord-mcp.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
