apiVersion: v1
kind: Service
metadata:
  name: discord-mcp
spec:
  type: ClusterIP
  selector:
    app: discord-mcp
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
