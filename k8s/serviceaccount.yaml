apiVersion: v1
kind: ServiceAccount
metadata:
  name: discord-mcp
# If you pull from a private registry (e.g. OCIR), uncomment and reference the
# pull secret you create (see README). It can live here or on the Deployment.
# imagePullSecrets:
#   - name: ocir-pull-secret
